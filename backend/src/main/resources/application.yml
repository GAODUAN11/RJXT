spring:
  datasource:
    url: jdbc:mysql://localhost:3306/graduate_platform?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 200662.Gd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  mail:
    host: smtp.qq.com
    port: 587
    username: 1345169715@qq.com
    password: kyhwvasgnqligjdi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

app:
  jwt:
    secret: graduate-platform-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
    expiration-ms: 86400000

logging:
  level:
    com.graduateplatform: DEBUG
